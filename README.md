CoursePM Reminder
========

Basic script to email examiner based on CSV-file to remind them of missing parts in CoursePM.

Expecting csv file, for example `data.csv` with headers according to code (see code or ask snIT16/17 for details)

Usage: `python3 reminder.py data.csv`  
